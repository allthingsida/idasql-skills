interface:
  display_name: "Resource"
  short_description: "Recover source-level understanding recursively"
  default_prompt: "Use $resource to recursively re-source this binary into clearer names, comments, and types."
